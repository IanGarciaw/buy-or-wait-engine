"""A3 — Multimodal + Mensajes.

Convierte 16 imágenes y 215 mensajes en hechos tipados (ImageFact, MessageFact de
contracts.py). El único punto de entrada para el resto del sistema es
`extraction.facts.load_facts`.
"""
